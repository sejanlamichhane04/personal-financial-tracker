# Shared Components & Utilities

Place any components or utility functions here that you want to use in both your React SPA (src/) and Next.js (app/) projects.

- Import from this folder using relative paths in both codebases.
- Keep these components framework-agnostic (no Next.js or Vite-specific code). 